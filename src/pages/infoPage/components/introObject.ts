import clockIcon from '../../../imgs/svgs/clock.svg';
import supportIcon from '../../../imgs/svgs/support.svg';

export const clock = {
  icon: clockIcon,
  description: 'sua loja aberta 24 horas por dia, toda semana',
};

export const support = {
  icon: supportIcon,
  description: 'suporte, rapido e pratico a todo momento',
};

const allObjects = [clock, support];

export default allObjects;
